*-*-*-*-*-*-* Your Teaching Digest for Today *-*-*-*-*-*-*
<%= Date.today.strftime("%a, %b%e, %Y") %>

Hello, <%= @user.first_name %>! Here is everything you need to rock those lessons today.

Happy teaching!

_________________________________

<% @user.todays_lessons.each do |lesson| %>

  <%= lesson.student.full_name %> <%= lesson.starts_at.in_time_zone(@user.timezone).strftime("%l:%M %P") %> (<%= lesson.duration %> min.)
  Previous Lesson (<%= lesson.student.past_lessons.first.starts_at.in_time_zone(@user.timezone).strftime("%a, %b%e, %Y") %>)

  <% if lesson.lesson_notes.present?%>
    <%= lesson.student.past_lessons.first.lesson_notes %>
  <% else %>
    This lesson doesn't have any notes.
  <% end %>

<% end %>
