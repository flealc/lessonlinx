<div class="h5 ms-3 mt-2">Lessons to log</div>

<% if current_user.past_lessons.where(status: :scheduled).any? %>

  <div class="list-group list-group-flush rounded-3 fw-normal small me-4 me-sm-0">
    <% current_user.past_lessons.scheduled.first(5).each do |lesson| %>
      <div class="list-group-item list-group-item-action">
        <%= lesson.student.full_name %>
        <div>
          <%= link_to "Log", edit_student_lesson_path(lesson.student, lesson, lesson: { status: "taught" }), class: "stretched-link", style: "color: black;" %>
          <%= lesson.starts_at.strftime("%b %d, %-l:%M %p") %>
        </div>
      </div>
    <% end %>
  </div>

<% else %>
  <div class="bg-body-tertiary h-100 d-flex justify-content-center align-items-center rounded-3">
    <div class="text-center text-secondary text-wrap mx-3">No lessons to log.</div>
  </div>
<% end %>
